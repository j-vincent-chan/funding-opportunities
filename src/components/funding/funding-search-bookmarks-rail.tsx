/** Sidebar footer — saved searches are managed in the chip strip above results. */
export function FundingSearchBookmarksRail() {
  return (
    <p className="mt-6 border-t border-[var(--fo-divider)] pt-5 text-[0.75rem] leading-snug text-[var(--fo-ink-muted)]">
      Saved searches appear above the results.
    </p>
  );
}
